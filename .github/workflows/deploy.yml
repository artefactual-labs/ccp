name: Deploy

on:
  workflow_run:
    workflows: ["Release"]
    types:
      - completed

jobs:
  deploy:
    permissions:
      contents: none
    runs-on: ubuntu-latest
    environment: prod
    concurrency: prod
    steps:
      - name: Deploy
        run: |
          echo hello world
